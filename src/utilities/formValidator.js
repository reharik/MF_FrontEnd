// /**
//  * Created by reharik on 3/20/16.
//  */
// import validation from './validation';
//
// export function validate(properties) {
//   var newProperties = properties.map(p=> ({...p, validation(p.validators, p.value) }));
//
//   return {isValid: newProperites.every(x=> x.valid), properties: newProperties};
// }
